services:
  imachess:
    build: .
  redis:
    image: redis
    ports:
      - "6379:6379"
  nginx:
    image: nginx:latest
    volumes:
      - "./nginx.conf:/etc/nginx/nginx.conf:ro"
    ports:
      - "80:80"
